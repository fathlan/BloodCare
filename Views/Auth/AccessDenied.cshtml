@{
    Layout = null;
}
<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="utf-8" />
    <title>Akses Ditolak - BloodCare</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" />
    <style>
        body {
            background: #f5f5f5;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: 'Segoe UI', sans-serif;
        }
        .box {
            text-align: center;
            background: #fff;
            padding: 50px;
            border-radius: 16px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
        }
        .box h1 { color: #b71c1c; font-size: 60px; margin: 0; }
    </style>
</head>
<body>
    <div class="box">
        <h1></h1>
        <h3>Akses Ditolak</h3>
        <p class="text-muted">Kamu tidak punya izin untuk membuka halaman ini.</p>
        <a href="/" class="btn btn-outline-danger">Kembali ke Beranda</a>
    </div>
</body>
</html>
